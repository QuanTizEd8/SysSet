{
   "id": "install-gh",
   "version": "0.1.0",
   "name": "GitHub CLI Installer",
   "description": "Install the GitHub CLI (gh). Two strategies controlled by 'method': 'repos' (default) adds and uses the official GitHub CLI package repository on Debian/Ubuntu (apt) and RHEL/Fedora/SUSE (rpm/dnf), the native community package on Alpine (apk) and Arch (pacman), and Homebrew on macOS; 'binary' downloads the pre-built binary from GitHub Releases for all platforms including Alpine (the Linux binaries are statically linked with CGO_ENABLED=0). Version is controlled by the 'version' option.",
   "options": {
      "version": {
         "type": "string",
         "default": "latest",
         "description": "Version of gh to install.\n'latest' (default) — with method=repos: install the latest version from the configured repository. With method=binary: query the GitHub Releases API for the newest release.\nA version string (e.g. '2.89.0') — with method=repos: on apt, passes 'gh=<version>' to apt-get. On macOS (Homebrew), Alpine, Arch, and RHEL-based systems (dnf/yum/zypper), version pinning is not supported: a warning is logged and the latest available package is installed instead. With method=binary: downloads exactly that release from github.com/cli/cli/releases.",
         "proposals": ["latest", "2.89.0", "2.88.1"]
      },
      "method": {
         "type": "string",
         "default": "repos",
         "description": "Installation strategy.\n'repos' (default) — configure and use the official GitHub CLI package repository. On Debian/Ubuntu: adds the official apt repo (cli.github.com/packages) with GPG keyring and installs via apt. On RHEL/Fedora/CentOS/Amazon Linux: adds the official rpm repo and installs via dnf/yum. On SUSE/openSUSE: adds the official rpm repo and installs via zypper. On Alpine: installs via 'apk add github-cli' (community package, no version pinning). On Arch: installs via 'pacman -S github-cli' (community package, no version pinning). On macOS: installs via Homebrew ('brew install gh').\n'binary' — download the pre-built binary archive from github.com/cli/cli/releases; verify SHA-256 against the release checksums file; install the 'gh' binary to install_path. The Linux release binaries are statically linked (CGO_ENABLED=0), so this method works on all Linux distributions including Alpine/musl without any compatibility shim. install_completions controls whether shell completions are also installed.",
         "enum": ["repos", "binary"]
      },
      "install_path": {
         "type": "string",
         "default": "/usr/local/bin",
         "description": "Directory where the 'gh' binary is installed when method=binary.\nDefaults to '/usr/local/bin'. Must be writable or creatable by the installer; the directory is created if it does not exist. Ignored when method=repos (the package manager controls the install location, typically /usr/bin)."
      },
      "symlink": {
         "type": "boolean",
         "default": true,
         "description": "Create a symlink /usr/local/bin/gh -> install_path/gh when install_path is not /usr/local/bin (method=binary only, root only).\nEnsures the containerEnv PATH entry (/usr/local/bin) resolves to the installed binary regardless of install_path.\nNo-op when install_path is already /usr/local/bin, when method=repos (gh lands in /usr/bin which is always on PATH), or when running as non-root (cannot write to /usr/local/bin)."
      },
      "install_completions": {
         "type": "boolean",
         "default": true,
         "description": "Install shell completions for bash and zsh.\nApplies to both methods.\nWith method=binary: completion files are taken from the release archive ('share/bash-completion/completions/gh' and 'share/zsh/site-functions/_gh').\nWith method=repos: completions are generated by running 'gh completion -s bash' and 'gh completion -s zsh' after installation.\nAs root: writes bash completion to '/etc/bash_completion.d/gh' and zsh completion to '<zshdir>/completions/_gh' (where <zshdir> is detected by shell__detect_zshdir).\nAs non-root: writes to '$HOME/.local/share/bash-completion/completions/gh' (bash) and '$HOME/.zfunc/_gh' (zsh)."
      },
      "if_exists": {
         "type": "string",
         "default": "skip",
         "description": "What to do when 'gh' is already present in PATH before installation begins.\nIf the installed version matches the resolved target version, installation is always skipped silently regardless of this setting.\n'skip' (default) — log a notice and exit successfully without making any changes.\n'fail' — print an error and exit non-zero.",
         "enum": ["skip", "fail"]
      },
      "extensions": {
         "type": "string",
         "default": "",
         "description": "Comma-separated list of GitHub CLI extensions to install after gh itself is installed. Each entry is passed verbatim to 'gh extension install', so any argument that command accepts is valid: a GitHub repository slug ('owner/repo'), a full https:// URL, or a local directory path. Example: 'dlvhdr/gh-dash,github/gh-copilot'. Extensions are installed for the users resolved by the add_*_user_config and add_user_config options. Set to '' to skip extension installation."
      },
      "add_current_user_config": {
         "type": "boolean",
         "default": true,
         "description": "Apply per-user git configuration (git_protocol, setup_git, sign_commits) and install extensions for the current non-root user (SUDO_USER if run via sudo, otherwise the result of whoami).\nNo effect when the current user is root and other non-root users are also being targeted.\nOnly applies when at least one of 'extensions', 'git_protocol', 'setup_git', or 'sign_commits' is active."
      },
      "add_remote_user_config": {
         "type": "boolean",
         "default": true,
         "description": "Apply per-user git configuration (git_protocol, setup_git, sign_commits) and install extensions for the devcontainer remoteUser (the _REMOTE_USER environment variable set by the devcontainer tooling).\nNo effect when _REMOTE_USER is not set.\nOnly applies when at least one of 'extensions', 'git_protocol', 'setup_git', or 'sign_commits' is active."
      },
      "add_container_user_config": {
         "type": "boolean",
         "default": true,
         "description": "Apply per-user git configuration (git_protocol, setup_git, sign_commits) and install extensions for the devcontainer containerUser (the _CONTAINER_USER environment variable set by the devcontainer tooling).\nNo effect when _CONTAINER_USER is not set.\nOnly applies when at least one of 'extensions', 'git_protocol', 'setup_git', or 'sign_commits' is active."
      },
      "add_user_config": {
         "type": "string",
         "default": "",
         "description": "Comma-separated list of additional usernames to apply per-user git configuration and install extensions for (e.g. 'alice,bob'). Each user must already exist. Root is accepted here if explicitly listed. Only applies when at least one of 'extensions', 'git_protocol', 'setup_git', or 'sign_commits' is active."
      },
      "git_protocol": {
         "type": "string",
         "default": "",
         "description": "Set the default git protocol in gh's per-user config (~/.config/gh/config.yml) by running 'gh config set git_protocol <value>' for each resolved user.\n'https' — use HTTPS for git clone and push operations (gh's built-in default).\n'ssh' — use SSH for git clone and push.\n'' (empty, default) — leave the config key unset; gh uses its own default (https).\nApplied for all users resolved by the add_*_user_config / add_user_config options.",
         "proposals": ["", "https", "ssh"]
      },
      "setup_git": {
         "type": "boolean",
         "default": false,
         "description": "Register gh as the git credential helper by running 'gh auth setup-git --force --hostname <git_hostname>' for each resolved user.\nThis writes a credential helper entry to ~/.gitconfig, allowing git operations to authenticate via gh without a separate credential store.\n'--force' is used so the command succeeds at container build time even when there is no active gh login yet.\nDefaulting to false to avoid unexpected changes to user gitconfig; set to true in devcontainer environments where gh will be the primary authentication mechanism."
      },
      "sign_commits": {
         "type": "string",
         "default": "",
         "description": "Pre-configure commit signing in each resolved user's ~/.gitconfig.\n'' (default) — skip; make no changes to commit signing config.\n'ssh' — set gpg.format=ssh and commit.gpgsign=true. Requires git 2.34+. Well-suited for devcontainers with SSH agent forwarding (via runArgs/remoteEnv in devcontainer.json): signing is silent and needs no TTY or pinentry. Users must still set user.signingkey themselves (e.g. via dotfiles: 'git config --global user.signingkey ~/.ssh/id_ed25519.pub'). For commits to show as Verified on GitHub, the corresponding public key must be registered under GitHub Settings > SSH and GPG keys > New SSH signing key.\n'gpg' — set commit.gpgsign=true and explicitly unset gpg.format so git uses its default GPG format. Suited for users with an existing GPG key and gpg-agent already configured. Users must set user.signingkey to their GPG key ID (e.g. 'git config --global user.signingkey 3AA5C34371567BD2'). Note: gpg-agent socket forwarding into containers requires additional host setup beyond standard devcontainer spec; SSH signing is simpler in container environments.\nIn both cases, 'user.signingkey' is intentionally not set by this feature because the key identifier is user-specific and unknown at build time.",
         "proposals": ["", "ssh", "gpg"]
      },
      "git_hostname": {
         "type": "string",
         "default": "github.com",
         "description": "The hostname to configure as the git credential helper target when setup_git=true.\nDefaults to 'github.com'. Set to your GitHub Enterprise Server hostname (e.g. 'git.corp.example.com') when using GHES.\nIgnored when setup_git=false."
      },
      "installer_dir": {
         "type": "string",
         "default": "/tmp/gh-install",
         "description": "Working directory used for method=binary: the binary archive and checksums file are downloaded and extracted here.\nRemoved automatically after a successful install unless no_clean=true.\nIgnored when method=repos."
      },
      "no_clean": {
         "type": "boolean",
         "default": false,
         "description": "Keep the installer_dir after a successful install when method=binary.\nUseful for debugging or inspecting the extracted archive.\nIgnored when method=repos."
      },
      "debug": {
         "type": "boolean",
         "default": false,
         "description": "Enable debug output (set -x)."
      },
      "logfile": {
         "type": "string",
         "default": "",
         "description": "Append install log to this file path."
      }
   },
   "containerEnv": {
      "PATH": "/usr/local/bin:${PATH}"
   }
}
