{
   "id": "install-miniforge",
   "version": "0.2.0",
   "name": "Miniforge Installer",
   "description": "Install Miniforge in the development container.",
   "options": {
      "version": {
         "default": "latest",
         "description": "Conda version to install (e.g. '26.3.1').\nAccepts a conda version string, not a Miniforge release tag.\nDefaults to 'latest', which resolves to the current latest release via the GitHub API.\nFor a specific version, the script finds the highest Miniforge build number for that conda version\n(e.g. '24.7.1' resolves to tag '24.7.1-2').\nIf the already-installed conda version matches the resolved version, the install is\nskipped silently regardless of the 'if_exists' setting.\n",
         "type": "string"
      },
      "if_exists": {
         "default": "skip",
         "description": "What to do when conda is already installed at prefix.\n'skip' — warn and continue to post-install steps (idempotent).\n'fail' — print an error and exit non-zero.\n'reinstall' — uninstall the existing installation then install fresh.\n'update' — update the base conda environment to the resolved version.\n",
         "enum": ["skip", "fail", "reinstall", "update"],
         "type": "string"
      },
      "preserve_envs": {
         "default": true,
         "description": "When if_exists is 'reinstall', export all non-base conda environments before\nuninstalling and recreate them after reinstalling.\nEach environment is exported with --from-history; on recreation failure the YAML\nis kept at /tmp/conda-env-preserve for manual recovery.\nDefaults to true.\n",
         "type": "boolean"
      },
      "preserve_config": {
         "default": true,
         "description": "When if_exists is 'reinstall', skip conda init --reverse and preserve .condarc\nand .conda directories during uninstall.\nSet to false to perform a full clean uninstall.\nDefaults to true.\n",
         "type": "boolean"
      },
      "update_base": {
         "default": false,
         "description": "Update the base conda environment.\nThis is done by running `conda update --all`.\nThis is not recommended for production environments.\n",
         "type": "boolean"
      },
      "prefix": {
         "default": "auto",
         "description": "Path to the conda installation directory (installation root/prefix).\n'auto' (default) — resolves to /opt/conda when running as root (the de-facto standard used by continuumio Docker images and the devcontainer ecosystem), or ~/miniforge3 when running as non-root (the Miniforge installer's own default prefix).\nAn explicit path — used as-is; the directory is created if it does not exist.\nWith symlink=true, a symlink is created from the canonical prefix to $PREFIX when they differ: /opt/conda -> $PREFIX for root, $HOME/miniforge3 -> $PREFIX for non-root.\n",
         "type": "string"
      },
      "export_path": {
         "default": "auto",
         "description": "Controls which shell startup files receive the PATH export for $PREFIX/bin.\n'auto' writes to all relevant system-wide files (Case A: public install + root)\nor user-scoped files (Case B: user install or non-root).\n'' (empty) skips all PATH writes.\nNewline-separated list of absolute paths: writes only to those files.\n",
         "type": "string"
      },
      "symlink": {
         "default": true,
         "description": "Create a directory symlink from the canonical conda prefix to $prefix when prefix resolves to a non-default path.\nRoot: creates /opt/conda -> $prefix. Non-root: creates $HOME/miniforge3 -> $prefix.\nEnsures the containerEnv PATH entries always resolve to the conda binaries regardless of the chosen prefix.\nNo-op when prefix already resolves to the canonical path (/opt/conda for root, $HOME/miniforge3 for non-root).",
         "type": "boolean"
      },
      "rc_files": {
         "default": "auto",
         "description": "Shell rc files to write conda initialization to.\n'auto' writes to system-wide rc files when running as root, or to the current user's\n~/.bashrc and $ZDOTDIR/.zshrc when running as non-root.\n'' (empty) skips all conda init writes.\nNewline-separated list of absolute paths: writes only to those files.\n",
         "type": "string"
      },
      "activate_env": {
         "default": "base",
         "description": "Conda environment to activate at shell startup.\n'base' sets auto_activate_base=true in ~/.condarc (the conda-native mechanism).\nAny other name appends a 'conda activate <env>' line to the rc files.\n'' (empty) skips activation setup entirely.\nOnly takes effect when rc_files is non-empty.\n",
         "type": "string"
      },
      "set_permissions": {
         "default": false,
         "description": "Set permissions for the conda installation directory.\nCreates the conda group, adds all users listed in `users` to it,\nand applies group ownership and group-write bits to the conda directory.\n",
         "type": "boolean"
      },
      "group": {
         "default": "conda",
         "description": "Name of a user group to give access to conda.\nOnly applies when `set_permissions` is true.\n",
         "type": "string"
      },
      "users": {
         "default": "",
         "description": "Comma-separated list of users to add to the conda group (e.g. 'alice,bob').\nEach user must already exist.\nOnly applies when `set_permissions` is true.\nIf empty, defaults to the user running the script.\n",
         "type": "string"
      },
      "interactive": {
         "default": false,
         "description": "Run the installer in interactive mode.\nThis allows the installer to prompt the user.\nThe default is to run the installer in non-interactive mode.\n",
         "type": "boolean"
      },
      "installer_dir": {
         "default": "/tmp/miniforge-installer",
         "description": "Path to a directory to download the installer to.\nThis is the directory where the Miniforge installer will be downloaded.\n",
         "type": "string"
      },
      "keep_installer": {
         "default": false,
         "description": "Keep the Miniforge installer script and checksum after installation instead of removing them.",
         "type": "boolean"
      },
      "debug": {
         "default": false,
         "description": "Enable debug output.",
         "type": "boolean"
      },
      "logfile": {
         "default": "",
         "description": "Log all output (stdout + stderr) to this file in addition to console.",
         "type": "string"
      }
   },
   "containerEnv": {
      "PATH": "${HOME}/miniforge3/bin:/opt/conda/bin:${PATH}"
   },
   "installsAfter": [
      // "ghcr.io/quantized8/sysset/install-shell",
      // "ghcr.io/quantized8/sysset/setup-user"
   ]
}
