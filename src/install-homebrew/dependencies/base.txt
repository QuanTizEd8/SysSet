# Build tools required by Homebrew on Linux.
# ref: https://docs.brew.sh/Homebrew-on-Linux#requirements
#
# Not needed on macOS — Xcode Command Line Tools provide these, and the
# installer checks / installs CLT directly in the script.

# Common packages — same name on all package managers
git
curl
file
gzip

# Build tools — names differ by ecosystem
build-essential  [pm=apt]
procps           [pm=apt] [pm=zypper]
procps-ng        [pm=dnf] [pm=pacman]

--- group [pm=dnf]
development-tools

--- group [pm=pacman]
base-devel

--- group [pm=zypper]
devel_basis
