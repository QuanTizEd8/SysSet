{
  "id": "install-homebrew",
  "version": "0.1.0",
  "name": "Install Homebrew",
  "description": "Installs Homebrew on macOS (and Linux). Ensures bash >=4 is available before handing off to the main installer.",
  "documentationURL": "https://github.com/quantized8/sysset/tree/main/src/install-homebrew",
  "options": {
    "debug": {
      "type": "boolean",
      "default": false,
      "description": "Enable debug output (set -x)."
    },
    "logfile": {
      "type": "string",
      "default": "",
      "description": "Append install log to this file path."
    }
  }
}
