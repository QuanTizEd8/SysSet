env:
  PATH: "/opt/homebrew/bin:/usr/local/bin:{env.PATH}"

pre-commit:
  commands:
    sync:
      glob: "{lib/*,bootstrap.sh}"
      run: make sync
    format:
      glob: "*.{sh,bash,bats}"
      run: make format FILES="{staged_files}"
