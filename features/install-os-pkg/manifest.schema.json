{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OS Package Manifest",
  "description": "Declarative YAML/JSON manifest for cross-platform OS package installation. Replaces the text-based manifest DSL. Consumed by ospkg__run() in lib/ospkg.bash.\n\nExecution order: prescripts → PM-block setup (keys, repos, taps, ppas, copr, modules) → package-manager update → packages → PM-block scripts → scripts → repo cleanup → cache clean.\n\nInline keys/repos/scripts on package entries and groups are collected and executed in the same batch order — structural co-location is for authoring convenience, not execution sequencing.",
  "oneOf": [
    {
      "description": "Shorthand: a bare non-empty array is treated as a top-level packages list.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/packageEntry"
      }
    },
    {
      "$ref": "#/$defs/manifestObject"
    }
  ],
  "$defs": {
    "manifestObject": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Optional manifest ID for documentation, logging, and debugging purposes."
        },
        "description": {
          "type": "string",
          "description": "Optional manifest description for documentation, logging, and debugging purposes."
        },
        "when": {
          "$ref": "#/$defs/WhenSpec",
          "description": "Top-level condition. If it does not match the active OS context, the entire manifest is skipped."
        },
        "packages": {
          "type": "array",
          "description": "Packages to install. Entries can be bare strings (package names), package objects (with PM overrides and conditions), or group objects (shared conditions/flags over multiple packages).",
          "items": {
            "$ref": "#/$defs/packageEntry"
          }
        },
        "keys": {
          "type": "array",
          "description": "Signing keys to fetch before adding repos or installing packages.",
          "items": {
            "$ref": "#/$defs/keyEntry"
          }
        },
        "repos": {
          "type": "array",
          "description": "Repository definitions to add before package installation (removed after unless keep_repos is set).",
          "items": {
            "$ref": "#/$defs/repoEntry"
          }
        },
        "ppas": {
          "type": "array",
          "description": "APT PPA identifiers (apt-add-repository). Effective only when the active PM is apt.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "taps": {
          "type": "array",
          "description": "Homebrew taps to add before installing packages. Effective only when the active PM is brew.",
          "items": {
            "$ref": "#/$defs/tapEntry"
          }
        },
        "copr": {
          "type": "array",
          "description": "DNF COPR repository names (e.g. 'user/repo'). The implementation applies these only when the detected package manager is `dnf` (including microdnf, which is treated as the dnf family). On systems where only `yum` is detected, entries here are ignored.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "modules": {
          "type": "array",
          "description": "DNF module streams to enable (e.g. 'nodejs:18'). The implementation applies these only when the detected package manager is `dnf` (including microdnf). On systems where only `yum` is detected, entries here are ignored.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "groups": {
          "type": "array",
          "description": "Package group names (dnf group install / zypper pattern / pacman group).",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "required": [
                  "name"
                ],
                "additionalProperties": false,
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Package group name in the active PM's group/pattern syntax."
                  },
                  "when": {
                    "$ref": "#/$defs/WhenSpec",
                    "description": "Install this group only when the condition matches."
                  }
                }
              }
            ]
          }
        },
        "casks": {
          "type": "array",
          "description": "Homebrew cask names to install. Effective only when the active PM is brew on macOS.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "prescripts": {
          "description": "Shell commands run before any package installation (after keys/repos setup).",
          "$ref": "#/$defs/scriptBlock"
        },
        "scripts": {
          "description": "Shell commands run after all packages are installed (before repo cleanup and cache clean).",
          "$ref": "#/$defs/scriptBlock"
        },
        "apt": {
          "$ref": "#/$defs/aptBlock"
        },
        "apk": {
          "$ref": "#/$defs/apkBlock"
        },
        "brew": {
          "$ref": "#/$defs/brewBlock"
        },
        "dnf": {
          "$ref": "#/$defs/dnfBlock"
        },
        "yum": {
          "$ref": "#/$defs/yumBlock"
        },
        "pacman": {
          "$ref": "#/$defs/pacmanBlock"
        },
        "zypper": {
          "$ref": "#/$defs/zypperBlock"
        }
      }
    },
    "WhenOperatorObject": {
      "description": "Explicit per-key operators. All present operators are ANDed. Use on version-like fields (`feat.version`, `os.version_id`, …) for semver ordering; ordering operators are ignored for `os.id_like`. Array values are allowed only on `eq` and `ne` (OR within that operator).",
      "type": "object",
      "minProperties": 1,
      "properties": {
        "eq": {
          "description": "Equal to (case-insensitive string match, or token membership for `os.id_like`).",
          "oneOf": [
            {"type": "string", "minLength": 1},
            {
              "type": "array",
              "items": {"type": "string", "minLength": 1},
              "minItems": 1
            }
          ]
        },
        "ne": {
          "description": "Not equal to (negation of `eq` semantics).",
          "oneOf": [
            {"type": "string", "minLength": 1},
            {
              "type": "array",
              "items": {"type": "string", "minLength": 1},
              "minItems": 1
            }
          ]
        },
        "lt": {
          "description": "Semver less-than.",
          "type": "string",
          "minLength": 1
        },
        "lte": {
          "description": "Semver less-than or equal.",
          "type": "string",
          "minLength": 1
        },
        "gt": {
          "description": "Semver greater-than.",
          "type": "string",
          "minLength": 1
        },
        "gte": {
          "description": "Semver greater-than or equal.",
          "type": "string",
          "minLength": 1
        }
      },
      "additionalProperties": false
    },
    "WhenConditionValue": {
      "description": "Condition value for one qualified key. A string or non-empty string array is shorthand for `eq` (array = OR). An operator object uses explicit `eq`/`ne`/`lt`/`lte`/`gt`/`gte` constraints.",
      "oneOf": [
        {"type": "string", "minLength": 1},
        {
          "type": "array",
          "items": {"type": "string", "minLength": 1},
          "minItems": 1
        },
        {"$ref": "#/$defs/WhenOperatorObject"}
      ]
    },
    "WhenConditionObject": {
      "description": "AND group: every key must match. Keys must be qualified (`os.id`, `plat.pm`, `feat.version`, …) and match `^(os|plat|feat)\\.[a-z0-9_]+$` (no case-flavor suffixes). Common keys: `os.*` from /etc/os-release, `plat.*` from host detection (`plat.pm`, `plat.kernel`, `plat.machine_release`, …), `feat.*` from install context (`feat.version`, `feat.method`, `feat.pm_version`, …).",
      "type": "object",
      "minProperties": 1,
      "propertyNames": {"pattern": "^(os|plat|feat)\\.[a-z0-9_]+$"},
      "additionalProperties": {"$ref": "#/$defs/WhenConditionValue"}
    },
    "WhenSpec": {
      "description": "Runtime when block evaluated by `ctx__match_when` / `ctx-match.jq` (`when_matches`, `item_when_matches`). Either one AND object, or a non-empty OR array of AND objects (any group matching is sufficient). Evaluated against merged OS/platform context plus optional `feat.*` keys supplied at install time.",
      "oneOf": [
        {"$ref": "#/$defs/WhenConditionObject"},
        {
          "description": "OR list: at least one AND group must match.",
          "type": "array",
          "items": {"$ref": "#/$defs/WhenConditionObject"},
          "minItems": 1
        }
      ]
    },
    "scriptBlock": {
      "description": "Shell command(s). A string is executed as a single script. An array of strings is joined with newlines before execution.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1
        }
      ]
    },
    "flagsValue": {
      "description": "Extra flags passed verbatim to the package manager's install command. A string is split on whitespace; an array is passed as-is.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1
        }
      ]
    },
    "keyEntry": {
      "type": "object",
      "description": "A signing key to download and install. Used for apt (gpg), rpm (rpm --import), pacman (pacman-key), etc. Requires at least one of `url` or `fingerprint`.",
      "required": [
        "dest"
      ],
      "anyOf": [
        {
          "required": [
            "url"
          ]
        },
        {
          "required": [
            "fingerprint"
          ]
        }
      ],
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to download the signing key from."
        },
        "fingerprint": {
          "type": "string",
          "pattern": "^[0-9A-Fa-f]{40}$",
          "description": "40-character GPG fingerprint. When provided without `url`, the key is fetched from a keyserver (Ubuntu HTTPS, then HKP fallbacks). When provided alongside `url`, future implementations may verify the downloaded key against this fingerprint."
        },
        "dest": {
          "type": "string",
          "minLength": 1,
          "description": "Destination file path for the key. If the path ends in .gpg, the key is automatically dearmored via gpg --dearmor."
        },
        "dearmor": {
          "type": "boolean",
          "description": "GPG dearmor when saving the key: `true` always pipes through `gpg --dearmor`; `false` always stores the raw download (if `dest` ends in .gpg, the file is written with a .key extension instead). When omitted, dearmoring is used when `dest` ends in .gpg (same as auto)."
        }
      }
    },
    "repoEntry": {
      "type": "string",
      "description": "Repository definition in the active PM's native format. Examples:\n  apt:    \"deb [signed-by=/usr/share/keyrings/foo.gpg] https://repo.example.com bookworm main\"\n  apk:    \"@community https://dl-cdn.alpinelinux.org/alpine/edge/community\"\n  dnf/yum: .repo file content or a baseurl string\n  pacman: INI section for /etc/pacman.conf\n  zypper: zypper addrepo arguments\n\nRepositories are added before package installation and removed after (unless keep_repos is set).",
      "minLength": 1
    },
    "packageEntry": {
      "description": "A package to install. Can be a bare string (package name), a package object, or a group object.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "description": "Bare package name, installed via the detected package manager."
        },
        {
          "$ref": "#/$defs/packageObject"
        },
        {
          "$ref": "#/$defs/groupObject"
        }
      ]
    },
    "packageObject": {
      "type": "object",
      "description": "A package with optional PM-specific name overrides, conditions, flags, and inline setup (keys, repos, scripts).\n\nThe 'name' field is the default package name. PM override keys (apt, brew, etc.) replace it when that PM is active. If every expected PM has an explicit override, 'name' acts as a label only (never passed to the PM).",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Default package name, or a label when all target PMs have explicit overrides."
        },
        "when": {
          "$ref": "#/$defs/WhenSpec",
          "description": "Condition filter. The package is skipped when the condition does not match."
        },
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "PATH guard: if this command is already on PATH at install time, the package is skipped — the dependency is treated as satisfied regardless of how the command was installed (a binary/cargo/npm install by another feature, or an existing system tool). Unlike the package-manager presence check, this works on every platform and honours non-PM installs. Only consulted on install/skip runs, not under `--update` (which refreshes deps), mirroring the built-in already-installed skip. Use the command a user would actually invoke (e.g. `rg` for the `ripgrep` package)."
        },
        "flags": {
          "$ref": "#/$defs/flagsValue",
          "description": "Extra flags for the PM's install command. Pair with a 'when' condition if the flags are PM-specific (e.g. --no-install-recommends for apt)."
        },
        "version": {
          "type": "string",
          "description": "Version constraint in the active PM's syntax (e.g. '=1.2.3-1' for apt, '>=1.2' for brew). May contain `{feat.pm_version}` or other context tokens expanded before install when the manifest is generated from feature metadata."
        },
        "prescript": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands collected and run in the prescript phase (before package installation)."
        },
        "script": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands collected and run in the script phase (after package installation)."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "Signing keys collected and fetched in the key phase (before repos)."
        },
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository definitions collected and added in the repo phase (after keys, before update)."
        }
      },
      "patternProperties": {
        "^(apt|apk|brew|dnf|yum|pacman|zypper)$": {
          "type": "string",
          "minLength": 1,
          "description": "Package name override for this package manager. When the active PM matches, this name is installed instead of 'name'."
        }
      }
    },
    "groupObject": {
      "type": "object",
      "description": "A group of packages that share conditions, flags, and/or inline setup. The group's 'when', 'flags', 'keys', 'repos', 'prescript', and 'script' apply to every package in the group. This is a logical tree for co-authoring, not a distro package group name (for dnf `group install` / zypper patterns, use the top-level `groups` key).\n\nGroups can nest: a group's 'packages' array can contain strings, package objects, or further groups. Optional 'label' is for human log messages only. When 'packages' is omitted, the group serves as a conditional grouping for keys, repos, and scripts without installing any packages.",
      "additionalProperties": false,
      "properties": {
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "minItems": 1,
          "description": "The packages in this group. Inherits the group's when/flags/keys/repos/scripts."
        },
        "label": {
          "type": "string",
          "description": "Human-readable label for log output. Not used for installation."
        },
        "when": {
          "$ref": "#/$defs/WhenSpec",
          "description": "Condition filter applied to all packages in the group. Combined (AND'd) with any per-package 'when' conditions."
        },
        "flags": {
          "$ref": "#/$defs/flagsValue",
          "description": "Extra flags applied to all packages in the group. Merged with any per-package flags."
        },
        "prescript": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands collected and run in the prescript phase."
        },
        "script": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands collected and run in the script phase."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "Signing keys collected and fetched in the key phase."
        },
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository definitions collected and added in the repo phase."
        }
      }
    },
    "aptBlock": {
      "type": "object",
      "description": "apt-specific setup. Only evaluated when apt is the detected package manager (Debian, Ubuntu, etc.).",
      "additionalProperties": false,
      "properties": {
        "ppas": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "PPAs to add via add-apt-repository. Example: 'ppa:deadsnakes/ppa'."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "Signing keys fetched before adding repositories."
        },
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository lines written to /etc/apt/sources.list.d/. Format: 'deb [signed-by=/path/to/key.gpg] https://repo.example.com suite component'."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when apt is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        }
      }
    },
    "apkBlock": {
      "type": "object",
      "description": "apk-specific setup. Only evaluated when apk is the detected package manager (Alpine Linux).",
      "additionalProperties": false,
      "properties": {
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "Signing keys to install to /etc/apk/keys/ before adding repositories."
        },
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository lines appended to /etc/apk/repositories. Example: '@community https://dl-cdn.alpinelinux.org/alpine/edge/community'."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when apk is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        }
      }
    },
    "brewBlock": {
      "type": "object",
      "description": "Homebrew-specific setup. Only evaluated when brew is the detected package manager (macOS, Linuxbrew).",
      "additionalProperties": false,
      "properties": {
        "taps": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/tapEntry"
          },
          "description": "Homebrew taps to add before installation."
        },
        "casks": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Homebrew casks to install (macOS GUI applications: .app bundles, .pkg installers). No Linux equivalent."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when brew is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        }
      }
    },
    "tapEntry": {
      "description": "A Homebrew tap. Can be a short name string ('user/repo') or an object with a custom URL.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "description": "Tap short name. Example: 'homebrew/cask', 'user/repo'."
        },
        {
          "type": "object",
          "required": [
            "name"
          ],
          "additionalProperties": false,
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "Tap name in 'user/repo' format."
            },
            "url": {
              "type": "string",
              "format": "uri",
              "description": "Custom Git URL for the tap (when not hosted on GitHub)."
            }
          }
        }
      ]
    },
    "dnfBlock": {
      "type": "object",
      "description": "dnf-specific setup. Only evaluated when dnf is the detected package manager (Fedora, RHEL 8+, etc.).",
      "additionalProperties": false,
      "properties": {
        "copr": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "COPR repositories to enable via dnf copr enable. Example: 'user/project'."
        },
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository configuration written to /etc/yum.repos.d/."
        },
        "modules": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "DNF modules to enable. Format: 'module:stream' or 'module:stream/profile'. Example: 'nodejs:18/common'."
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "DNF package groups to install. Example: 'development-tools'."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "RPM signing keys to import via rpm --import."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when dnf is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        }
      }
    },
    "yumBlock": {
      "type": "object",
      "description": "yum-specific setup. Only evaluated when yum is the detected package manager (RHEL 7, CentOS 7, Amazon Linux, etc.).",
      "additionalProperties": false,
      "properties": {
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository configuration written to /etc/yum.repos.d/."
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Yum package groups to install."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "RPM signing keys to import via rpm --import."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when yum is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        }
      }
    },
    "pacmanBlock": {
      "type": "object",
      "description": "pacman-specific setup. Only evaluated when pacman is the detected package manager (Arch Linux, Manjaro, etc.).",
      "additionalProperties": false,
      "properties": {
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repository configuration appended to /etc/pacman.conf. INI-style section format."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "PGP keys to receive via pacman-key --recv-keys and locally sign."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when pacman is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Pacman package groups to install. Example: 'base-devel'."
        }
      }
    },
    "zypperBlock": {
      "type": "object",
      "description": "zypper-specific setup. Only evaluated when zypper is the detected package manager (openSUSE, SLES, etc.).",
      "additionalProperties": false,
      "properties": {
        "repos": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/repoEntry"
          },
          "description": "Repositories to add via zypper addrepo."
        },
        "keys": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/keyEntry"
          },
          "description": "GPG keys to import for package verification."
        },
        "scripts": {
          "$ref": "#/$defs/scriptBlock",
          "description": "Shell commands run when zypper is the active PM."
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/packageEntry"
          },
          "description": "Packages to install when this PM is active."
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Zypper patterns to install via 'zypper install -t pattern'. Example: 'devel_basis'."
        }
      }
    }
  }
}
