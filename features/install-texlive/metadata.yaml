version: 0.1.0
name: TeX Live Installation
description: >-
  Install [TeX Live](https://www.tug.org/texlive/) via the official TeX Live installer script or the system package manager.
keywords:
  - latex
  - tex
  - texlive
  - pdflatex
  - xelatex
  - lualatex
  - typesetting
  - document
  - tlmgr
_long_description: |-
  [TeX Live](https://www.tug.org/texlive/) is the comprehensive cross-platform
  LaTeX/TeX distribution maintained by TeX user groups. It ships TeX engines
  (pdfLaTeX, XeLaTeX, LuaLaTeX, plain TeX), an extensive library of macro packages
  and fonts, and `tlmgr` — the package manager for ongoing CTAN package lifecycle.

  Two installation paths are supported:

  - **`script`** (default) — The [official TeX Live Unix installer](https://www.tug.org/texlive/quickinstall.html),
    downloaded from CTAN. Works on Linux (glibc and musl/Alpine) and macOS. Provides
    full control over the installation scheme, target directory, repository mirror, paper size, and
    whether to include documentation and source. The installer is driven by a generated
    `texlive.profile` file built from all individual profile-key options; use `profile` to supply a
    fully custom profile that takes complete precedence. Use `packages` to add individual CTAN
    packages on top of the chosen scheme.

  - **`package`** — Install distro-packaged TeX Live from the system package manager. The `scheme`
    option influences the package set selected where the distro offers multiple granularity levels.
    Version is controlled by the distribution's package policy.
_options:
  verify:
    args: "version"
    description: Verify the TeX Live installation via tlmgr.
    functional:
      description: tlmgr reads its TeX Live configuration
      # `tlmgr conf` dumps the resolved texmf trees and tlmgr configuration from
      # the local TeX Live database — a read-only command exercising real config
      # resolution offline, with no package-repository access.
      cmd: '{bin} conf > /dev/null 2>&1'
  user_file_fetch: true
  method:
    script:
      asset_uri: ''
    package:
      when: {plat.pm: apk}
  version:
    resolution: none
    default: latest
    description: >-
      TeX Live version to install. Accepts: `stable` (most recent annual frozen
      release, auto-detected by probing historic mirrors), `latest` (live CTAN
      mirror pool), a 4-digit year (e.g. `2025`), or a date
      (e.g. `2025-04-15` for a specific daily snapshot).
    proposals:
      - value: stable
        description: Most recent annual TeX Live release (frozen snapshot; probes current then previous release year).
      - value: latest
        description: Live CTAN mirror pool — always the most current TeX Live packages.
      - value: '2026'
        description: TeX Live 2026 frozen snapshot.
      - value: '2025'
        description: TeX Live 2025 frozen snapshot.
      - value: '2024'
        description: TeX Live 2024 frozen snapshot.
  prefix:
    root: /usr/local/texlive
    nonroot: ${HOME}/texlive
    bins: [tlmgr]
    symlink:
      skip: true
    exports:
      skip: true
options:
  scheme:
    type: string
    default: small
    description: |-
      TeX Live installation scheme — the bundle of packages to include. For `method=script`,
      sets the `selected_scheme scheme-<value>` key in the installer profile. Also influences
      package selection for `method=package` where distro packaging allows multiple granularity
      levels.
      Has no effect when `profile` is provided (the profile's `selected_scheme` takes precedence)
      or when `collections` is non-empty — the TeX Live installer resets the scheme's collection
      list when any `collection-*` line is present; only the explicitly listed collections are
      installed. See `collections` for details.
    enum:
      - value: full
        description: >-
          [Full scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-full.tlpsrc?view=markup) –
          Complete TeX Live distribution: all packages, engines, and fonts (~7 GB).
          Choose when any LaTeX document must build without missing-package errors.
      - value: medium
        description: >-
          [Medium scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-medium.tlpsrc?view=markup) –
          LaTeX plus a large set of commonly used extra packages and fonts (~2 GB). A practical
          default for general document production workflows.
      - value: small
        description: >-
          [Small scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-small.tlpsrc?view=markup) –
          LaTeX plus a curated set of recommended packages (~400 MB). Good balance between
          functionality and image size for most document workflows; extend with `packages`.
      - value: basic
        description: >-
          [Basic scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-basic.tlpsrc?view=markup) –
          LaTeX plus fundamental packages only (~200 MB). Suitable when you want a working base
          and will add exactly the CTAN packages needed via `packages`.
      - value: minimal
        description: >-
          [Minimal scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-minimal.tlpsrc?view=markup) –
          TeX engine and bare-minimum formats only (~100 MB). Pair with `packages` to build a
          purpose-specific installation. `latex` and most macro packages are absent until added.
      - value: infraonly
        description: >-
          [Infrastructure-only scheme](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-infraonly.tlpsrc?view=markup) –
          Only `tlmgr` infrastructure, no TeX packages (~30 MB). Use when you want full control
          over exactly which packages are present by listing them in `packages`. The `latex` binary
          will not be present until you add `latex-bin` (and friends) to `packages`.

  packages:
    type: array
    default: latexmk
    description: |-
      Additional CTAN packages to install via `tlmgr install` after the base scheme or
      package-manager installation. Each line is a CTAN package name
      (e.g. `latexmk`, `biber`, `pgf`, `collection-fontsrecommended`).
      Applied for all methods that include a working `tlmgr` installation
      (`script` and `package` where `tlmgr` is writable).
    proposals:
      - value: latexmk
        description: '`latexmk` — automated LaTeX build tool; required by VS Code LaTeX Workshop.'
      - value: "latexmk\nbiber"
        description: '`latexmk` + `biber` — build automation and modern bibliography backend (BibLaTeX).'
      - value: "latexmk\nbiber\ncollection-fontsrecommended"
        description: '`latexmk` + `biber` + the recommended CTAN fonts collection.'
      - value: "latexmk\nbiber\ncollection-latexextra"
        description: '`latexmk` + `biber` + the large `collection-latexextra` package collection (~500 MB additional packages).'
      - value: "latexmk\nbiber\ncollection-fontsrecommended\nxetex\nlualatex-math"
        description: '`latexmk` + `biber` + recommended fonts + XeTeX + LuaLaTeX math support.'
      - value: ''
        description: Install no additional packages beyond the chosen scheme.

  collections:
    type: array
    default: ''
    _applies_when:
      - method: [script]
    description: |-
      TeX Live collections to install (without the `collection-` prefix). When any collections
      are listed, `scheme-infraonly` is written as the profile scheme and ONLY the specified
      collections are installed. This reflects documented TeX Live installer behaviour: the
      presence of any `collection-*` line in the profile causes the installer to reset the
      scheme's default collection list before applying the profile — meaning the scheme's
      collections are never added. `scheme-infraonly` is used explicitly to make the intent
      unambiguous. The `scheme` option is therefore ignored when `collections` is non-empty.
      Leave empty (default) to install the collections bundled with the chosen `scheme`.
      To add individual CTAN packages on top of a scheme (without replacing its collections),
      use `packages`.
      The full list of TeX Live collections is defined in the
      [TUG SVN package database](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/scheme-full.tlpsrc?view=markup).
    enum:
      - value: basic
        description: >-
          [collection-basic](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-basic.tlpsrc?view=markup) –
          Essential programs and files.
      - value: bibtexextra
        description: >-
          [collection-bibtexextra](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-bibtexextra.tlpsrc?view=markup) –
          BibTeX additional styles
      - value: binextra
        description: >-
          [collection-binextra](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-binextra.tlpsrc?view=markup) –
          TeX auxiliary programs
      - value: context
        description: >-
          [collection-context](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-context.tlpsrc?view=markup) –
          ConTeXt and packages
      - value: fontsextra
        description: >-
          [collection-fontsextra](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-fontsextra.tlpsrc?view=markup) –
          Additional fonts
      - value: fontsrecommended
        description: >-
          [collection-fontsrecommended](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-fontsrecommended.tlpsrc?view=markup) –
          Recommended fonts
      - value: fontutils
        description: >-
          [collection-fontutils](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-fontutils.tlpsrc?view=markup) –
          Graphics and font utilities
      - value: formatsextra
        description: >-
          [collection-formatsextra](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-formatsextra.tlpsrc?view=markup) –
          Additional formats
      - value: games
        description: >-
          [collection-games](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-games.tlpsrc?view=markup) –
          Games typesetting
      - value: humanities
        description: >-
          [collection-humanities](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-humanities.tlpsrc?view=markup) –
          Humanities packages
      - value: langarabic
        description: >-
          [collection-langarabic](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langarabic.tlpsrc?view=markup) –
          Arabic
      - value: langchinese
        description: >-
          [collection-langchinese](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langchinese.tlpsrc?view=markup) –
          Chinese
      - value: langcjk
        description: >-
          [collection-langcjk](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langcjk.tlpsrc?view=markup) –
          Chinese/Japanese/Korean (base)
      - value: langcyrillic
        description: >-
          [collection-langcyrillic](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langcyrillic.tlpsrc?view=markup) –
          Cyrillic
      - value: langczechslovak
        description: >-
          [collection-langczechslovak](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langczechslovak.tlpsrc?view=markup) –
          Czech/Slovak
      - value: langenglish
        description: >-
          [collection-langenglish](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langenglish.tlpsrc?view=markup) –
          US and UK English
      - value: langeuropean
        description: >-
          [collection-langeuropean](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langeuropean.tlpsrc?view=markup) –
          Other European languages
      - value: langfrench
        description: >-
          [collection-langfrench](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langfrench.tlpsrc?view=markup) –
          French
      - value: langgerman
        description: >-
          [collection-langgerman](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langgerman.tlpsrc?view=markup) –
          German
      - value: langgreek
        description: >-
          [collection-langgreek](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langgreek.tlpsrc?view=markup) –
          Greek
      - value: langitalian
        description: >-
          [collection-langitalian](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langitalian.tlpsrc?view=markup) –
          Italian
      - value: langjapanese
        description: >-
          [collection-langjapanese](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langjapanese.tlpsrc?view=markup) –
          Japanese
      - value: langkorean
        description: >-
          [collection-langkorean](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langkorean.tlpsrc?view=markup) –
          Korean
      - value: langother
        description: >-
          [collection-langother](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langother.tlpsrc?view=markup) –
          Other languages
      - value: langpolish
        description: >-
          [collection-langpolish](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langpolish.tlpsrc?view=markup) –
          Polish
      - value: langportuguese
        description: >-
          [collection-langportuguese](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langportuguese.tlpsrc?view=markup) –
          Portuguese
      - value: langspanish
        description: >-
          [collection-langspanish](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-langspanish.tlpsrc?view=markup) –
          Spanish
      - value: latex
        description: >-
          [collection-latex](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-latex.tlpsrc?view=markup) –
          LaTeX fundamental packages
      - value: latexextra
        description: >-
          [collection-latexextra](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-latexextra.tlpsrc?view=markup) –
          LaTeX additional packages
      - value: latexrecommended
        description: >-
          [collection-latexrecommended](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-latexrecommended.tlpsrc?view=markup) –
          LaTeX recommended packages
      - value: luatex
        description: >-
          [collection-luatex](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-luatex.tlpsrc?view=markup) –
          LuaTeX packages
      - value: mathscience
        description: >-
          [collection-mathscience](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-mathscience.tlpsrc?view=markup) –
          Mathematics, natural sciences, computer science packages
      - value: metapost
        description: >-
          [collection-metapost](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-metapost.tlpsrc?view=markup) –
          MetaPost and Metafont packages
      - value: music
        description: >-
          [collection-music](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-music.tlpsrc?view=markup) –
          Music packages
      - value: pictures
        description: >-
          [collection-pictures](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-pictures.tlpsrc?view=markup) –
          Graphics, pictures, diagrams
      - value: plaingeneric
        description: >-
          [collection-plaingeneric](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-plaingeneric.tlpsrc?view=markup) –
          Plain (La)TeX packages
      - value: pstricks
        description: >-
          [collection-pstricks](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-pstricks.tlpsrc?view=markup) –
          PSTricks
      - value: publishers
        description: >-
          [collection-publishers](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-publishers.tlpsrc?view=markup) –
          Publisher styles, theses, etc.
      - value: texworks
        description: >-
          [collection-texworks](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-texworks.tlpsrc?view=markup) –
          TeXworks editor
      - value: wintools
        description: >-
          [collection-wintools](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-wintools.tlpsrc?view=markup) –
          Windows-only support programs (no-op on Linux/macOS)
      - value: xetex
        description: >-
          [collection-xetex](https://svn.tug.org:8369/texlive/trunk/Master/tlpkg/tlpsrc/collection-xetex.tlpsrc?view=markup) –
          XeTeX and packages

  profile:
    type: string
    default: ''
    description: |
      Inline `texlive.profile` content or path/URI to a profile file for `method=script`.
      When non-empty, the profile is used directly as the installer profile and all individual
      profile-key options (`prefix`, `scheme`, `collections`, `texmflocal`, `texmfsysvar`,
      `texmfsysconfig`, `texmfhome`, `texmfvar`, `texmfconfig`, `instopt_adjustpath`,
      `instopt_adjustrepo`, `instopt_letter`, `instopt_portable`, `instopt_write18_restricted`,
      `tlpdbopt_install_docfiles`, `tlpdbopt_install_srcfiles`, `tlpdbopt_sys_bin`,
      `tlpdbopt_sys_man`, `tlpdbopt_sys_info`, `tlpdbopt_create_formats`, `tlpdbopt_post_code`,
      `tlpdbopt_generate_updmap`, `tlpdbopt_autobackup`, `tlpdbopt_backupdir`) are ignored — the
      profile controls these settings completely. Non-profile options (`repository`,
      `verify_downloads`, `packages`, `generate_caches`, `tlmgr_update`, `tlmgr_verify_repo`,
      `version`, `if_exists`, `installer_dir`) remain active.

      A profile is a plain-text file with `key value` pairs, one per line. Unknown keys cause the
      installer to abort. If any `collection-*` key is present, the installer installs only the
      explicitly listed collections (the scheme's default collection list is NOT expanded).
      The flags `--repository` and `--no-verify-downloads` are CLI flags, not profile keys; use
      the corresponding feature options (`repository`, `verify_downloads`) instead.

      Inline content is detected when the value contains a newline; otherwise the value is treated
      as a file path, `file://` / `http(s)://` / `oci://` / `gh://owner/repo@ref:path` URI.
      Append `#sha256=<hex>` for integrity verification. Use `fetch_headers` and `fetch_netrc`
      for authentication.
    proposals:
      - value: /workspaces/${localWorkspaceFolderBasename}/.devcontainer/texlive.profile
        description: Profile file in the mounted workspace.
      - value: https://raw.githubusercontent.com/owner/repo/main/texlive.profile
        description: Public GitHub raw URL.
      - value: gh://owner/repo@main:.devcontainer/texlive.profile
        description: Profile from a GitHub repository (`gh://` form).
      - value: |
          selected_scheme scheme-small
          tlpdbopt_install_docfiles 0
          tlpdbopt_install_srcfiles 0
          instopt_adjustpath 1
          tlpdbopt_sys_bin /usr/local/bin
        description: Inline profile (newline in value).

  repository:
    type: string
    _applies_when:
      - method: [script]
    default: ctan
    description: |-
      TeX Live package repository to use as the source of installation.
      Set an explicit URL to:
      - Avoid mirror-selection variability (use a specific, trusted mirror).
      - Install from a historic yearly snapshot (pin to an older TeX Live release).
      - Use a local directory or internal network cache for air-gapped environments.
    proposals:
      - value: ctan
        description: Alias for the installer default (auto-selected CTAN mirror).
      - value: https://mirror.ctan.org/systems/texlive/tlnet
        description: |
          Installer default — auto-selected [CTAN](https://ctan.org) mirror
          using https://mirror.ctan.org/systems/texlive/tlnet.
      - value: https://ftp.math.utah.edu/pub/tex/historic/systems/texlive/2025/tlnet-final
        description: Explicit CTAN mirror URL. The list of CTAN mirrors is available at https://ctan.org/mirrors/.
      - value: ftp://server/path/to/tlnet
        description: Explicit mirror with `ftp://` protocol.
      - value: scp://user@machine/path/to/tlnet
        description: Remote mirror accessible via `scp` (SSH).
      - value: ssh://user@machine/path/to/tlnet
        description: Equivalent way to specify a remote mirror with `scp`.
      - value: user@machine:/path/to/tlnet
        description: Equivalent way to specify a remote mirror with `scp`.
      - value: /path/to/local/tlnet
        description: Local directory containing the TeX Live repository tree.
      - value: file:/path/to/local/tlnet
        description: Equivalent way to specify a local directory.


  verify_downloads:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Verify the integrity of downloaded TeX Live packages using GPG signatures. When true
      (default), the installer's built-in signature verification is active — it auto-detects
      `gpg` in PATH and skips verification gracefully if not found. Set to false to pass
      `--no-verify-downloads` to the installer, disabling signature checks unconditionally.
      Useful in air-gapped environments where the TeX Live GPG infrastructure is unreachable.

  tlmgr_update:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Run `tlmgr update --self --all` after the initial installation to pull in the latest
      package updates from the configured repository. Disabled by default to keep the
      installation reproducible and deterministic. Enable in development images where the latest
      CTAN state is always desired.

  generate_caches:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Generate ConTeXt file-database / LuaTeX cache and font caches after installation.
      Disabled by default because cache generation adds significant build time and is only
      needed when ConTeXt documents will be compiled inside the container. When enabled,
      runs `mtxrun --generate`, `context --make`, and `fc-cache -fsv`. Only meaningful
      when ConTeXt is included in the installation (e.g. `scheme=full`, `scheme=context`,
      or `context` listed in `collections`).

  tlmgr_verify_repo:
    type: string
    _applies_when:
      - method: [script]
    default: main
    description: |-
      Set the `tlmgr` repository-signature verification level via `tlmgr option verify-repo
      <value>` after installation. Controls which repositories must present a valid GPG signature
      before `tlmgr` will install packages from them.
    enum:
      - value: none
        description: No signature verification for any repository.
      - value: main
        description: Verify signatures from the main TeX Live repository only; third-party repositories are not required to be signed.
      - value: all
        description: Require valid signatures from all repositories including third-party ones.

  # ── script profile options ───────────────────────────────────────────────
  # The following options map directly to keys in the texlive.profile file that
  # drives the install-tl (TeX Live Unix installer) script. They are all ignored when `profile` is set.

  # -- directory keys --

  texmflocal:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      System-wide tree for local additions (packages, fonts, styles) shared across all users.
      Leave empty (default) to use `<prefix>/texmf-local` (e.g. `/usr/local/texlive/texmf-local`),
      kept inside the TeX Live prefix and not removed when the TeX Live tree is rebuilt.
      Ignored when `profile` is provided (profile key `TEXMFLOCAL`).
    proposals:
      - value: ''
        description: Installer default (`/usr/local/texlive/texmf-local`).
      - value: /opt/texmf-local
        description: Custom system-wide local tree outside the TeX Live prefix.

  texmfsysconfig:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `TEXMFSYSCONFIG` — the system-wide directory for configuration files
      (e.g. `updmap.cfg`, `fmtutil.cnf`) that apply to all users.
      Leave empty (default) to use `<prefix>/texmf-config` (inside the TeX Live prefix).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Default (`<prefix>/texmf-config`).

  texmfsysvar:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `TEXMFSYSVAR` — the system-wide directory where the installer writes generated
      files (format dumps, font maps, updmap output) that apply to all users.
      Leave empty (default) to use `<prefix>/texmf-var` (inside the TeX Live prefix).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Default (`<prefix>/texmf-var`).

  texmfhome:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `TEXMFHOME` — the per-user directory for personal macro files, fonts, and
      styles. Leave empty (default) to use the installer default (`~/texmf`). Useful when the
      container user's home is non-standard or a project-relative path is preferred.
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (`~/texmf`).
      - value: /workspaces/${localWorkspaceFolderBasename}/.texmf
        description: Project-scoped TEXMFHOME inside the mounted workspace.

  texmfconfig:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `TEXMFCONFIG` — the per-user directory for TeX Live configuration files
      (e.g. personal `updmap.cfg`).
      Leave empty (default) to use the installer default (`~/.texlive<year>/texmf-config`).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (`~/.texlive<year>/texmf-config`).

  texmfvar:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `TEXMFVAR` — the per-user directory where TeX Live caches generated files
      (format dumps, font maps) for the current user.
      Leave empty (default) to use the installer default (`~/.texlive<year>/texmf-var`).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (`~/.texlive<year>/texmf-var`).

  # -- instopt_* keys --

  instopt_adjustpath:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Profile key `instopt_adjustpath` — whether the installer creates symlinks for all TeX Live
      binaries (and man/info pages) in `tlpdbopt_sys_bin` during installation. When true
      (default, `instopt_adjustpath 1`), TeX Live binaries are reachable from a stable directory
      without year- or architecture-specific path components in `containerEnv` or shell startup
      files. Set to false (`instopt_adjustpath 0`) when PATH is managed externally. Note: `instopt_portable=true` forces this off regardless.
      Ignored when `profile` is provided.

  instopt_adjustrepo:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Profile key `instopt_adjustrepo` — whether `tlmgr` adjusts the configured repository URL
      to match the installed TeX Live year after installation. When true (default,
      `instopt_adjustrepo 1`), prevents version mismatches when the same profile is reused across
      years. Set to false only when pinning to a historic snapshot repository that must not be
      auto-adjusted.
      Ignored when `profile` is provided.

  instopt_letter:
    type: string
    default: a4
    description: |-
      Profile key `instopt_letter` — the default paper size (`0` = A4, `1` = Letter). For
      `method=script`, written into the profile. For other methods where `tlmgr` is
      available, applied via `tlmgr option paper <value>` after installation.
      Ignored when `profile` is provided (set `instopt_letter` in the profile directly).
    enum:
      - value: a4
        description: A4 paper (210 × 297 mm) — international default (`instopt_letter 0`).
      - value: letter
        description: US Letter paper (8.5 × 11 in) — North American default (`instopt_letter 1`).

  instopt_portable:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Profile key `instopt_portable` — whether to produce a self-contained portable installation.
      When true (`instopt_portable 1`), the installer skips all system integration: no PATH
      symlinks (`instopt_adjustpath` is forced off), no file associations, no desktop integration,
      no multi-user Windows configuration. Use for installations that must be fully relocatable
      (e.g. embedded in a project directory). In standard container builds, leave false (default).
      Ignored when `profile` is provided.

  instopt_write18_restricted:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Profile key `instopt_write18_restricted` — whether the `\write18` shell-escape primitive is
      restricted to an approved list of commands. When true (default, `instopt_write18_restricted 1`),
      only whitelisted commands (e.g. `bibtex`, `biber`, `makeindex`) can be invoked; arbitrary
      shell commands are blocked. Set to false only for TeX documents that explicitly require
      unrestricted shell-escape (rare; consider the security implications carefully).
      Ignored when `profile` is provided.

  # -- tlpdbopt_* keys --

  tlpdbopt_install_docfiles:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Profile key `tlpdbopt_install_docfiles` — whether to install package documentation files.
      Documentation files add several hundred MB. Disable (the default, `tlpdbopt_install_docfiles 0`)
      for CI and headless container images; enable for developer workstation images that use local
      TeX docs. Ignored when `profile` is provided.

  tlpdbopt_install_srcfiles:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Profile key `tlpdbopt_install_srcfiles` — whether to install package source files. Source
      files are rarely needed in container environments. Enable only for TeX package development
      or when debugging macro package internals.
      Ignored when `profile` is provided.

  tlpdbopt_sys_bin:
    type: string
    _applies_when:
      - method: [script]
    default: auto
    description: |-
      Profile key `tlpdbopt_sys_bin` — the directory where the installer creates symlinks for
      all TeX Live binaries during installation. Only meaningful when `instopt_adjustpath=true`.
      `auto` resolves to `/usr/local/bin` as root or `~/.local/bin` as non-root at install time.
      An explicit absolute path is used verbatim; the directory is created if it does not exist.
      Ignored when `profile` is provided.
    proposals:
      - value: auto
        description: '`/usr/local/bin` as root, `~/.local/bin` as non-root.'
      - value: /usr/local/bin
        description: System-wide bin directory (root only).

  tlpdbopt_sys_man:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `tlpdbopt_sys_man` — the directory where the installer creates symlinks for
      TeX Live man pages during installation. Only meaningful when `instopt_adjustpath=true`.
      Leave empty (default) to use the installer default, which is derived from `tlpdbopt_sys_bin`
      (e.g. `/usr/local/share/man` when `tlpdbopt_sys_bin=/usr/local/bin`).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (derived from `tlpdbopt_sys_bin`).
      - value: /usr/local/share/man
        description: Conventional system-wide man directory.

  tlpdbopt_sys_info:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `tlpdbopt_sys_info` — the directory where the installer creates symlinks for
      TeX Live GNU info pages during installation. Only meaningful when `instopt_adjustpath=true`.
      Leave empty (default) to use the installer default, which is derived from `tlpdbopt_sys_bin`
      (e.g. `/usr/local/share/info` when `tlpdbopt_sys_bin=/usr/local/bin`).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (derived from `tlpdbopt_sys_bin`).
      - value: /usr/local/share/info
        description: Conventional system-wide info directory.

  tlpdbopt_create_formats:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Profile key `tlpdbopt_create_formats` — whether to generate TeX format files (`.fmt`)
      during installation. When true (default, `tlpdbopt_create_formats 1`), format generation
      makes the first `pdflatex`/`latex` invocation significantly faster. Set to false only when
      building a base image layer where format generation will be deferred to a later layer or
      container startup. Ignored when `profile` is provided.

  tlpdbopt_post_code:
    type: boolean
    _applies_when:
      - method: [script]
    default: true
    description: |-
      Profile key `tlpdbopt_post_code` — whether to run post-installation Perl code for each
      package (e.g. generating font maps, updmap, and other post-install hooks). When true
      (default, `tlpdbopt_post_code 1`), all post-install hooks fire during installation.
      Disabling speeds up installation at the cost of requiring a manual
      `tlmgr postaction install --all` run afterwards.
      Ignored when `profile` is provided.

  tlpdbopt_generate_updmap:
    type: boolean
    _applies_when:
      - method: [script]
    default: false
    description: |-
      Profile key `tlpdbopt_generate_updmap` — whether to regenerate `updmap.cfg` from scratch
      during installation rather than relying on per-package post-install hooks. Leave false
      (default, `tlpdbopt_generate_updmap 0`) in almost all cases; the standard post-install code
      controlled by `tlpdbopt_post_code` handles font map generation correctly. Enable only if
      directed by TeX Live documentation for a specific installation scenario.
      Ignored when `profile` is provided.

  tlpdbopt_autobackup:
    type: string
    _applies_when:
      - method: [script]
    default: '0'
    description: |-
      Profile key `tlpdbopt_autobackup` — the number of `tlmgr` package backup copies to retain
      so that `tlmgr restore` can roll back an update. `0` (default) — no backups; `1` — keep
      one backup per package; `-1` — unlimited backups. Defaults to `0` because container images
      rarely run `tlmgr update`, so backup files only waste space.
      Ignored when `profile` is provided.
    proposals:
      - value: '0'
        description: No backups (default; smallest footprint; cannot roll back).
      - value: '1'
        description: One backup per package.
      - value: '3'
        description: Keep the last three backups per package.
      - value: '-1'
        description: Unlimited backups.

  tlpdbopt_backupdir:
    type: string
    _applies_when:
      - method: [script]
    default: ''
    description: |-
      Profile key `tlpdbopt_backupdir` — the directory where `tlmgr` stores package backups
      when `tlmgr update` or `tlmgr install` replaces an existing package. Leave empty (default)
      to use the installer default (`TEXDIR/tlpkg/backups`).
      Ignored when `profile` is provided.
    proposals:
      - value: ''
        description: Installer default (`TEXDIR/tlpkg/backups`).
      - value: /var/cache/texlive/backups
        description: Custom backup directory outside the TeX Live tree.
customizations:
  vscode:
    extensions:
      - james-yu.latex-workshop
    settings:
      github.copilot.chat.codeGeneration.instructions:
        - text: >-
            This dev container has TeX Live installed with `latex`, `pdflatex`, `xelatex`,
            `lualatex`, and `tlmgr` available on the PATH. For building LaTeX documents,
            prefer `latexmk` (pre-installed) which handles multi-pass compilation and
            bibliography processing automatically.


_dependencies:
  run:
    base:
      packages:
        - ca-certificates
        - fontconfig

        # Perl: tlmgr and many TL tools are Perl scripts.
        # macOS ships with system Perl; skip brew install.
        - name: perl
          when: {plat.kernel: linux}

        # GnuPG: required at runtime for 'tlmgr key add/remove/list'.
        # gnupg (apt/apk/brew) pulls in gpg, gpg-agent, and dirmngr; dnf uses gnupg2.
        - name: gnupg
          dnf: gnupg2

        # PostScript/PDF interpreter: epstopdf, ps2pdf, pdfcrop, dvips → pdf, etc.
        - ghostscript

        # ncurses: texconfig TUI and xindy require the shared library.
        - name: libncurses6
          apk: ncurses-libs
          dnf: ncurses-libs
          brew: ncurses

        # Python Pygments: required by the minted LaTeX package for syntax highlighting.
        # python3 is a transitive dependency pulled in automatically.
        - name: python3-pygments
          apk: py3-pygments
          brew: pygments

        # Gnuplot (headless): pgfplots gnuplot terminal backend.
        # apt: gnuplot-nox excludes Qt/X11 deps; dnf: gnuplot-minimal is the headless build.
        - name: gnuplot
          apt: gnuplot-nox
          dnf: gnuplot-minimal

        # GNU FreeFont: broad Unicode coverage for documents using non-Latin characters.
        # dnf ships these as split packages (gnu-free-{mono,sans,serif}-fonts) and TL
        # itself includes texlive-gnu-freefont, so we only need to declare it for apt/apk.
        - name: fonts-freefont-otf
          when: {plat.pm: [apt, apk]}
          apk: font-freefont

        # zip + tidy: required by tex4ebook (HTML/epub output from LaTeX).
        - zip
        - name: tidy
          apk: tidyhtml
          brew: tidy-html5

        # ── latexindent Perl module dependencies ──────────────────────────────────
        # ref: https://latexindent.pl/documentation/
        # These modules are not part of the core Perl distribution on Linux.
        # On macOS they are provided by the system Perl or cpan; no brew formulae exist.
        - name: perl-unicode-linebreak
          when: {plat.kernel: linux}
          apt: libunicode-linebreak-perl
          apk: perl-unicode-linebreak
          dnf: perl-Unicode-LineBreak
        - name: perl-file-homedir
          when: {plat.kernel: linux}
          apt: libfile-homedir-perl
          apk: perl-file-homedir
          dnf: perl-File-HomeDir
        - name: perl-yaml-tiny
          when: {plat.kernel: linux}
          apt: libyaml-tiny-perl
          apk: perl-yaml-tiny
          dnf: perl-YAML-Tiny
        # Not in Alpine stable (edge/testing only); skip apk.
        - name: perl-getopt-long-descriptive
          when: {plat.pm: [apt, dnf]}
          apt: libgetopt-long-descriptive-perl
          dnf: perl-Getopt-Long-Descriptive
        # Digest::MD5 ships with perl itself on Fedora; explicit package needed on apt/apk.
        - name: perl-digest-md5
          when: [{plat.pm: apt}, {plat.pm: apk}]
          apt: libdigest-perl-md5-perl
          apk: perl-digest-md5
        # PDF::API2: required by the LaTeX memoize package.
        - name: perl-pdf-api2
          when: {plat.kernel: linux}
          apt: libpdf-api2-perl
          apk: perl-pdf-api2
          dnf: perl-PDF-API2

        # ── Linux-only X11 / OpenGL / RPC runtime libraries ───────────────────────
        # libsm6: X Session Management; required by MetaFont.
        - name: libsm
          when: {plat.kernel: linux}
          apt: libsm6
          apk: libsm
          dnf: libSM

        # freeglut / libtirpc / libcurl: required by asymptote.
        # apt names are version-dependent (time_t64 ABI transition in Debian trixie /
        # Ubuntu 24.04+) — handled in the apt: block below.
        # apk and dnf use stable names across versions.
        - name: freeglut
          when: {plat.pm: [apk, dnf]}
        - name: libtirpc
          when: {plat.pm: [apk, dnf]}
        - name: libcurl
          when: {plat.pm: [apk, dnf]}

        # ── Alpine-only runtime libraries ────────────────────────────────────────
        # On musl/Alpine, shared libraries that TL binaries link against are not
        # pulled in transitively by higher-level packages (unlike on glibc systems
        # where e.g. ghostscript or luatex drag them in automatically). Declare
        # them explicitly. Sourced from production Alpine TL Docker images.
        - name: cairo
          when: {plat.pm: apk}
        - name: icu-libs
          when: {plat.pm: apk}
        - name: libgcc
          when: {plat.pm: apk}
        - name: libpaper
          when: {plat.pm: apk}
        - name: libpng
          when: {plat.pm: apk}
        - name: libstdc++
          when: {plat.pm: apk}
        - name: libx11
          when: {plat.pm: apk}
        - name: pixman
          when: {plat.pm: apk}
        - name: zlib
          when: {plat.pm: apk}

      # apt-specific packages — handled here rather than in the cross-PM list above
      # because Debian bookworm+ / Ubuntu 22.04+ renamed several packages via the
      # time_t64 (64-bit time_t ABI) transition and OpenGL library reorganisation:
      #   libcurl4   → libcurl4t64    (jammy/bookworm → noble/trixie)
      #   freeglut3  → libglut3.12    (jammy/bullseye → bookworm/noble)
      #   libtirpc3  → libtirpc3t64   (jammy/bookworm → noble/trixie)
      apt:
        packages:
          # libcurl
          - name: libcurl4t64
            when: {os.version_codename: [noble, trixie, oracular, plucky, questing]}
          - name: libcurl4
            when: {os.version_codename: [bookworm, jammy, focal, bullseye]}
          # freeglut: renamed to libglut3.12 starting with bookworm/noble
          - name: libglut3.12
            when: {os.version_codename: [bookworm, noble, trixie, oracular, plucky, questing]}
          - name: freeglut3
            when: {os.version_codename: [jammy, focal, bullseye]}
          # libtirpc
          - name: libtirpc3t64
            when: {os.version_codename: [noble, trixie, oracular, plucky, questing]}
          - name: libtirpc3
            when: {os.version_codename: [bookworm, jammy, focal, bullseye]}
          # python-is-python3: creates /usr/bin/python → python3 symlink so that tools
          # invoking 'python' (older minted, some helper scripts) work out of the box.
          - python-is-python3
        # Asymptote links against libglut.so.3 but Debian/Ubuntu ships libglut.so.3.12.
        # Create the missing symlink so asymptote can load the library.
        scripts: |
          _libdir="$(find /usr/lib -type d -name '*-linux-*' 2>/dev/null | head -1)"
          if [[ -n "${_libdir}" && -f "${_libdir}/libglut.so.3.12" && ! -e "${_libdir}/libglut.so.3" ]]; then
            ln -sf "${_libdir}/libglut.so.3.12" "${_libdir}/libglut.so.3"
          fi

    option-jre:
      # Installed on-demand by _tl_maybe_install_jre when Java tool scripts (arara,
      # bib2gls, texplate, albatross, latex2nemeth, …) are found in the TL tree.
      packages:
        - name: default-jre-headless
          apk: openjdk21-jre-headless
          dnf: java-21-openjdk-headless
          yum: java-21-openjdk-headless
          zypper: java-21-openjdk-headless
          pacman: jre-openjdk-headless
          brew: openjdk

  build:
    method-script:
      # xz is required by the TeX Live installer to decompress .xz package archives
      # downloaded from CTAN during installation.
      # Users who need to run 'tlmgr install' or 'tlmgr update' after image build
      # should set keep_build_deps=true so xz remains available at runtime.
      # GPG is NOT listed here: the installer auto-detects gpg in PATH and skips
      # signature verification gracefully when not found; the verify_downloads option
      # passes --no-verify-downloads to disable it unconditionally.
      packages:
        - name: xz
          apt: xz-utils
