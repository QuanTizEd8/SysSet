version: 0.1.0
name: just Installation
description: Install [just](https://just.systems/) via release assets, package repositories, or Cargo.
keywords:
  - just
  - task-runner
  - build
  - cli
_long_description: |-
  Installs the `just` command runner with configurable installation methods:
  GitHub release assets, distro package repositories, and Cargo-based installation.
options: {}
_options:
  verify:
    args: "--version"
    description: Verify just is installed and working.
    functional:
      description: just runs a simple recipe
      cmd: '{bin} --justfile <(printf "hello:\n\t@echo hi\n") hello | grep -q hi'
  gh_repo: casey/just
  version:
    resolution: github_release
  method:
    binary:
      asset_uri: "https://github.com/${{ _options.gh_repo }}$/releases/download/{feat.tag}/just-{feat.version}-{plat.rust_triple}.tar.gz"
      binary_src:
        - just
      sidecar_uri: "https://github.com/${{ _options.gh_repo }}$/releases/download/{feat.tag}/SHA256SUMS"
    package:
      when: {plat.pm: [apt, dnf, apk, brew]}
    cargo:
      crate: just
  prefix:
    bins: [just]
    applies_when:
      - method: [binary, cargo]
customizations:
  vscode:
    extensions:
      - nefrob.vscode-just-syntax
_dependencies:
  run:
    method-package:
      packages:
        - name: just
          version: "{feat.pm_version}"
# installsAfter: []
