{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "scenarios.schema.json",
  "title": "Feature test scenarios",
  "description": "Test matrix for a feature under test/features/<feature-id>/: environments, modes, options, and which generated test scripts to run.",
  "type": "object",
  "minProperties": 1,
  "properties": {
    "defaults": {
      "description": "Per-feature defaults merged into every scenario (lowest precedence after test/features/defaults.shared.yaml).",
      "allOf": [
        {
          "$ref": "#/$defs/scenario"
        },
        {
          "not": {
            "required": [
              "expect_install_failure"
            ]
          }
        }
      ]
    },
    "generation": {
      "description": "Directives controlling how this feature's hand-written scenarios/checks interact with generated ones from .dev/lib/proman/test/gen/. Never itself a scenario.",
      "$ref": "#/$defs/generation"
    }
  },
  "patternProperties": {
    "^(?!defaults$|generation$)[a-z][a-z0-9_]*$": {
      "description": "One named scenario in the feature test matrix.",
      "allOf": [
        {
          "$ref": "#/$defs/scenario"
        },
        {
          "required": [
            "tests"
          ]
        }
      ]
    }
  },
  "additionalProperties": false,
  "$defs": {
    "generation": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "suppress": {
          "description": "Explicitly removes generated scenarios/check-groups for this feature. The only mechanism by which a generated and a hand-written name may share a name — without an entry here, a collision is a hard error, not a silent override.",
          "type": "object",
          "additionalProperties": false,
          "minProperties": 1,
          "properties": {
            "scenarios": {
              "description": "Generated scenario names to drop entirely for this feature.",
              "type": "array",
              "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*$"}
            },
            "checks": {
              "description": "Generated check-group names to drop entirely for this feature.",
              "type": "array",
              "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*$"}
            },
            "check_items": {
              "description": "Title prefixes of individual generated check items to drop from whichever generated group contains them, keeping the rest of the group. Finer-grained than `checks`; prefix-matched so one entry covers a title that embeds a per-scenario path (e.g. a multi-entrypoint tool's on-PATH-resolves assertion).",
              "type": "array",
              "items": {"type": "string", "minLength": 1}
            }
          }
        },
        "augment_tests": {
          "description": "Appends hand-authored checks.yaml group ids to a generated scenario's `tests:` list, so a scenario stays generated (envs/options/modes fully mechanical) while gaining feature-specific deepened assertions on top of the generated baseline checks.",
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "additionalProperties": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]*$"}
          }
        }
      }
    },
    "string_map": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "scenario": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "Human-readable summary of what this scenario exercises.",
          "type": "string"
        },
        "envs": {
          "description": "Environment keys from test/environments.yaml. May be inherited from defaults.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-z0-9][a-z0-9._+-]*$"
          }
        },
        "modes": {
          "description": "Test execution modes. Defaults to [devcontainer, standalone] when omitted.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "devcontainer",
              "standalone",
              "macos"
            ]
          }
        },
        "tests": {
          "description": "Test script IDs from checks.yaml (without a .sh suffix).",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        },
        "options": {
          "description": "Feature option key/value pairs merged with defaults (scenario wins).",
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_-]*$"
          },
          "additionalProperties": {
            "description": "Feature option value as passed to the installer.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "boolean"
              },
              {
                "type": "integer"
              },
              {
                "type": "number"
              },
              {
                "type": "null"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          }
        },
        "setup": {
          "description": "Shell commands run inside the container before install. In devcontainer mode these are baked into the generated Dockerfile as RUN layers.",
          "type": "string"
        },
        "expect_install_failure": {
          "description": "When true, the installer must exit non-zero. The runner validates install output against every kind: install_failure pattern in the scenario checks. In devcontainer mode the test container never starts, so only install_failure checks may be present when devcontainer is listed in modes.",
          "const": true
        },
        "fast_net_fail": {
          "description": "When true, set DEVFEATS_NET_FETCH_RETRIES=1 and DEVFEATS_NET_FETCH_DELAY=0 for the install so unreachable-host fetch failures finish quickly. Use for expected network fetch failures when standalone.network is not none. standalone.network: none implies this automatically.",
          "const": true
        },
        "args": {
          "description": "Additional CLI arguments forwarded to install.sh (rare; prefer options).",
          "$ref": "#/$defs/string_map"
        },
        "env_vars": {
          "description": "Extra environment variables set for the install invocation.",
          "$ref": "#/$defs/string_map"
        },
        "test_shell": {
          "description": "Shell invocation style for generated test scripts in standalone mode.",
          "type": "string",
          "enum": [
            "login",
            "nonlogin"
          ]
        },
        "devcontainer": {
          "description": "Mode-specific overrides applied when generating devcontainer.json test projects.",
          "type": "object",
          "additionalProperties": false,
          "minProperties": 1,
          "properties": {
            "remoteUser": {
              "description": "devcontainer.json remoteUser for the test project.",
              "type": "string",
              "minLength": 1
            },
            "containerUser": {
              "description": "devcontainer.json containerUser for the test project.",
              "type": "string",
              "minLength": 1
            }
          }
        },
        "standalone": {
          "description": "Mode-specific overrides for standalone Docker test runs.",
          "type": "object",
          "additionalProperties": false,
          "minProperties": 1,
          "properties": {
            "user": {
              "description": "Run generated test scripts as this user (install still runs as root unless skip_install is used).",
              "type": "string",
              "minLength": 1
            },
            "sudo": {
              "description": "When false, do not use sudo when switching to standalone.user.",
              "type": "boolean"
            },
            "network": {
              "description": "Docker network mode for the standalone container.",
              "const": "none"
            },
            "skip_install": {
              "description": "When true, the runner does not invoke install.sh; the test script must install explicitly.",
              "type": "boolean"
            }
          }
        }
      }
    }
  }
}
