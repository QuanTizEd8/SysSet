# Every scenario is now auto-generated from features/install-ripgrep/metadata.yaml
# (_options.verify.functional, _options.version.test_pins, _options.method,
# _options.prefix, if_exists, invalid enum values, log_file) by
# .dev/lib/proman/test/gen/. See test/features/generation.yaml for the
# generator config, and run `proman-test-gen-preview install-ripgrep` to
# inspect the generated output.
#
# The prior hand-written binary_custom_prefix scenario combined a custom
# prefix with if_exists: reinstall, but declared no setup: seeding a
# pre-existing install — so if_exists was inert (nothing to reinstall over),
# making it a fresh custom-prefix install identical in substance to the
# generated custom_prefix_symlink.
