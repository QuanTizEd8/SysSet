# ── base images ──────────────────────────────────────────────────────────────
# Environment keys use semantic tracks (stable/current), not version numbers.
# Image pins live in image: fields only — bump pins here without renaming keys.
#
#   stable  — primary LTS baseline (Ubuntu, Debian dual-track only)
#   current — version tracked in CI (all single-track families; newer dual-track slot)

ubuntu-stable:
  image: ubuntu:24.04
  attributes: {os.id: ubuntu, os.id_like: [debian], plat.pm: apt, plat.machine_release: [amd64, arm64]}

ubuntu-current:
  image: ubuntu:26.04
  attributes: {os.id: ubuntu, os.id_like: [debian], plat.pm: apt, plat.machine_release: [amd64, arm64]}

debian-stable:
  image: debian:12
  attributes: {os.id: debian, plat.pm: apt, plat.machine_release: [amd64, arm64]}

debian-current:
  image: debian:13
  attributes: {os.id: debian, plat.pm: apt, plat.machine_release: [amd64, arm64]}

alpine-current:
  image: alpine:3.24
  attributes: {os.id: alpine, plat.pm: apk, plat.machine_release: [amd64, arm64]}

fedora-current:
  image: fedora:44
  attributes: {os.id: fedora, plat.pm: dnf, plat.machine_release: [amd64, arm64]}

rockylinux-current:
  image: rockylinux/rockylinux:10
  attributes: {os.id: rocky, os.id_like: [rhel, fedora], plat.pm: dnf, plat.machine_release: [amd64, arm64]}

opensuse-leap-current:
  image: opensuse/leap:16.0
  attributes: {os.id: opensuse-leap, plat.pm: zypper, plat.machine_release: [amd64, arm64]}

archlinux-current:
  image: archlinux:base-20260621.0.546891
  attributes: {os.id: arch, plat.pm: pacman, plat.machine_release: [amd64]}

# Base image is condaforge/miniforge3 (Debian-based); attributes reflect that
# base OS. This environment exists to provide a pre-installed conda/mamba
# state for specific hand-written scenarios, not as a member of the generic
# envselect pool.
miniforge-current:
  image: condaforge/miniforge3:26.3.2-3
  attributes: {os.id: debian, plat.pm: apt, plat.machine_release: [amd64, arm64]}

# ── +bash variants ────────────────────────────────────────────────────────────
# Bash is not pre-installed on Alpine, Rocky Linux, or openSUSE Leap.
# These +bash variants are required wherever the test shim or devcontainer runner
# needs bash. Ubuntu, Debian, Fedora, and Arch Linux ship bash by default; no
# +bash needed for those. (archlinux:base includes bash via the base package group.)

alpine-current+bash:
  from: alpine-current
  build:
    dockerfile: retry apk add --no-cache bash

rockylinux-current+bash:
  from: rockylinux-current
  build:
    dockerfile: retry dnf install -y bash

opensuse-leap-current+bash:
  from: opensuse-leap-current
  build:
    # zypper exits 106 (ZYPPER_EXIT_INF_REPOS_SKIPPED) when some repos are
    # unreachable but the operation succeeds — treat that as success so a
    # genuine network failure (any other nonzero exit) is what triggers retry.
    dockerfile: |
      _zypper_install_bash() {
        set +e
        zypper install -y bash
        _rc=$?
        set -e
        [ "$_rc" -eq 0 ] || [ "$_rc" -eq 106 ]
      }
      retry _zypper_install_bash

fedora-current+bash:
  from: fedora-current
  build:
    dockerfile: retry dnf install -y bash

debian-stable+bash:
  from: debian-stable
  build:
    dockerfile: retry apt-get update -qq && retry apt-get install -y --no-install-recommends bash && rm -rf /var/lib/apt/lists/*

debian-current+bash:
  from: debian-current
  build:
    dockerfile: retry apt-get update -qq && retry apt-get install -y --no-install-recommends bash && rm -rf /var/lib/apt/lists/*

# ── +shell variants ───────────────────────────────────────────────────────────
# Base images augmented with a single non-bash shell or helper tool.

ubuntu-stable+fish:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends fish
      rm -rf /var/lib/apt/lists/*

ubuntu-stable+zsh:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update \
       && retry apt-get install -y --no-install-recommends zsh \
       && apt-get clean \
       && apt-get dist-clean 2>/dev/null || rm -rf /var/lib/apt/lists/*

ubuntu-stable+node:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends nodejs npm
      rm -rf /var/lib/apt/lists/*

ubuntu-stable+curl:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends curl ca-certificates bash
      rm -rf /var/lib/apt/lists/*

# ── +tool[-version] variants ──────────────────────────────────────────────────
# Base images with a specific tool pre-installed.
# Versioned variants ({base}+{tool}-{version}) are used for if_exists testing
# and for providing a known pre-installed state.

ubuntu-stable+git:
  from: ubuntu-stable
  build:
    dockerfile: retry apt-get update && retry apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*

ubuntu-stable+git+sudo:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends git sudo
      rm -rf /var/lib/apt/lists/*

ubuntu-stable+ripgrep:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends ripgrep
      rm -rf /var/lib/apt/lists/*

ubuntu-current+rust:
  # Rust is installed via rustup (the official installer), not the distro's apt
  # package: Ubuntu's packaged rustc/cargo lag upstream releases and routinely
  # fall short of the MSRV required by fast-moving crates (e.g. ruff, uv),
  # causing `cargo install` to fail with "requires rustc X.Y or newer". rustup
  # always provides the current stable toolchain, matching how a real user
  # would satisfy this method's precondition in practice.
  from: ubuntu-current
  build:
    dockerfile: |
      retry apt-get update -qq
      DEBIAN_FRONTEND=noninteractive retry apt-get install -y --no-install-recommends \
        curl ca-certificates gcc g++ make pkg-config
      rm -rf /var/lib/apt/lists/*
      curl --proto '=https' --tlsv1.2 -sSf --retry 60 --retry-delay 5 --retry-connrefused \
        https://sh.rustup.rs \
        | sh -s -- -y --profile minimal --default-toolchain stable
  env_vars:
    PATH: /root/.cargo/bin:$PATH

ubuntu-stable+jq-1.8.1:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends curl ca-certificates
      apt-get clean
      rm -rf /var/lib/apt/lists/*
      ARCH="$(uname -m)"
      case "${ARCH}" in
        x86_64)  JQ_ARCH="amd64" ;;
        aarch64) JQ_ARCH="arm64" ;;
        *) echo "Unsupported arch: ${ARCH}" && exit 1 ;;
      esac
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
        -H 'User-Agent: devfeats' \
        "https://github.com/jqlang/jq/releases/download/jq-1.8.1/jq-linux-${JQ_ARCH}" \
        -o /usr/local/bin/jq
      chmod +x /usr/local/bin/jq

ubuntu-stable+yq-4.44.2:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends curl ca-certificates
      apt-get clean
      apt-get dist-clean 2>/dev/null || rm -rf /var/lib/apt/lists/*
      ARCH="$(uname -m)"
      case "${ARCH}" in
        x86_64)  YQ_ARCH="amd64" ;;
        aarch64) YQ_ARCH="arm64" ;;
        *)        echo "Unsupported arch: ${ARCH}" && exit 1 ;;
      esac
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
        -H 'User-Agent: devfeats' \
        "https://github.com/mikefarah/yq/releases/download/v4.44.2/yq_linux_${YQ_ARCH}" \
        -o /usr/local/bin/yq
      chmod +x /usr/local/bin/yq

ubuntu-stable+gh-2.67.0:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update
      retry apt-get install -y --no-install-recommends curl ca-certificates
      rm -rf /var/lib/apt/lists/*
      ARCH="$(uname -m)"
      case "${ARCH}" in
        x86_64)  ASSET_ARCH="amd64" ;;
        aarch64) ASSET_ARCH="arm64" ;;
        *)       ASSET_ARCH="amd64" ;;
      esac
      GH_VERSION="2.67.0"
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
           -H 'User-Agent: devfeats' \
           -o /tmp/gh.tar.gz \
           "https://github.com/cli/cli/releases/download/v${GH_VERSION}/gh_${GH_VERSION}_linux_${ASSET_ARCH}.tar.gz"
      tar -xz -C /tmp -f /tmp/gh.tar.gz
      install -m 755 "/tmp/gh_${GH_VERSION}_linux_${ASSET_ARCH}/bin/gh" /usr/local/bin/gh
      rm /tmp/gh.tar.gz

ubuntu-stable+homebrew:
  from: ubuntu-stable
  build:
    dockerfile: |
      retry apt-get update \
       && retry apt-get install -y --no-install-recommends \
              git curl file build-essential procps ca-certificates \
       && apt-get clean \
       && apt-get dist-clean 2>/dev/null || rm -rf /var/lib/apt/lists/*
      useradd --system -m -s /bin/bash linuxbrew
      runuser -u linuxbrew -- env NONINTERACTIVE=1 /bin/bash -c \
          "$(curl -fsSL --retry 60 --retry-delay 5 --retry-connrefused \
              https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

debian-current+just:
  from: debian-current
  build:
    dockerfile: retry apt-get update -qq && retry apt-get install -y --no-install-recommends just && rm -rf /var/lib/apt/lists/*

debian-stable+direnv:
  from: debian-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends direnv
      rm -rf /var/lib/apt/lists/*

# ── +toolchain variants ───────────────────────────────────────────────────────
# Base images with build toolchains installed (compilers, build systems, dev headers).
# Named by what is installed, not by which feature uses it, to encourage reuse.

debian-stable+autotools:
  from: debian-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends \
        make automake autoconf libtool build-essential curl ca-certificates
      rm -rf /var/lib/apt/lists/*

alpine-current+build-base+ncurses:
  from: alpine-current
  build:
    # build-base + ncurses-dev: required for source builds of bash, zsh, and
    # other tools that use autotools + ncurses.
    dockerfile: retry apk add --no-cache build-base ncurses-dev curl ca-certificates

debian-stable+build-essential+ncurses:
  from: debian-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends \
        build-essential libncurses-dev curl ca-certificates xz-utils
      rm -rf /var/lib/apt/lists/*

ubuntu-stable+build-essential+git-dev-libs:
  from: ubuntu-stable
  build:
    # build-essential + git's specific C library requirements (libcurl, libexpat,
    # libpcre2, libssl, zlib) needed for compiling git from a kernel.org tarball.
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends \
        build-essential gettext libcurl4-openssl-dev libexpat1-dev \
        libpcre2-dev libssl-dev zlib1g-dev libpcre2-posix3 \
        curl ca-certificates tar jq yq
      rm -rf /var/lib/apt/lists/*

debian-stable+go:
  from: debian-stable
  build:
    dockerfile: |
      retry apt-get update -qq
      retry apt-get install -y --no-install-recommends make curl ca-certificates
      rm -rf /var/lib/apt/lists/*
      ARCH="$(uname -m)"
      case "${ARCH}" in
        x86_64)  GO_ARCH="amd64" ;;
        aarch64) GO_ARCH="arm64" ;;
        *) echo "Unsupported arch: ${ARCH}" && exit 1 ;;
      esac
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
        -H 'User-Agent: devfeats' \
        "https://go.dev/dl/go1.24.0.linux-${GO_ARCH}.tar.gz" \
        | tar -xz -C /usr/local
      ln -sf /usr/local/go/bin/go /usr/local/bin/go
      ln -sf /usr/local/go/bin/gofmt /usr/local/bin/gofmt

# ── +user variants ───────────────────────────────────────────────────────────
# Base images with a non-root user pre-created. Using pre-defined environments
# for user setup avoids repeating useradd lines across many scenario setup: blocks
# and eliminates the need for rolling-tag devcontainers-base images.
#
# ubuntu:24.04 ships with an 'ubuntu' user at UID 1000. All ubuntu-stable+*+vscode
# environments delete that user first so that vscode occupies UID 1000 consistently.
# This avoids UID collisions when scenarios create additional users (e.g. testuser at
# UID 1001) in their setup blocks, and prevents `useradd -u 1000 vscode` from failing.

ubuntu-stable+vscode:
  from: ubuntu-stable
  build:
    dockerfile: |
      userdel ubuntu
      useradd -m -s /bin/bash -u 1000 vscode

ubuntu-stable+git+sudo+vscode:
  from: ubuntu-stable+git+sudo
  build:
    dockerfile: |
      userdel ubuntu
      useradd -m -s /bin/bash -u 1000 vscode
      echo "vscode ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/vscode
      chmod 440 /etc/sudoers.d/vscode

debian-stable+vscode:
  from: debian-stable
  build:
    dockerfile: useradd -m -s /bin/bash -u 1000 vscode

alpine-current+build-base+ncurses+vscode:
  from: alpine-current+build-base+ncurses
  build:
    dockerfile: adduser -D -s /bin/sh -u 1000 vscode

debian-stable+build-essential+ncurses+vscode:
  from: debian-stable+build-essential+ncurses
  build:
    dockerfile: useradd -m -s /bin/bash -u 1000 vscode

ubuntu-stable+build-essential+git-dev-libs+vscode:
  from: ubuntu-stable+build-essential+git-dev-libs
  build:
    dockerfile: |
      userdel ubuntu
      useradd -m -s /bin/bash -u 1000 vscode

# ── from:-chain variants ──────────────────────────────────────────────────────
# Environments derived from other environments via the `from:` key.

ubuntu-stable+miniforge:
  from: ubuntu-stable+curl
  build:
    dockerfile: |
      ARCH="$(uname -m)"
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
        -H 'User-Agent: devfeats' \
        --output /tmp/miniforge.sh \
        "https://github.com/conda-forge/miniforge/releases/${MINIFORGE_PATH}/${MINIFORGE_FILENAME}-${ARCH}.sh"
      bash /tmp/miniforge.sh -b -p /opt/conda
      rm /tmp/miniforge.sh
  env_vars:
    PATH: /opt/conda/bin:${PATH}

ubuntu-stable+pixi:
  from: ubuntu-stable+curl
  build:
    dockerfile: |
      ARCH="$(uname -m)"
      case "${ARCH}" in
        x86_64)  TRIPLE="x86_64-unknown-linux-musl" ;;
        aarch64) TRIPLE="aarch64-unknown-linux-musl" ;;
        *)       TRIPLE="${ARCH}-unknown-linux-musl" ;;
      esac
      curl -fsSL --compressed --retry 60 --retry-delay 5 --retry-connrefused \
        -H 'User-Agent: devfeats' \
        "https://github.com/prefix-dev/pixi/releases/${PIXI_REF}/pixi-${TRIPLE}.tar.gz" \
        | tar -xz -C "${PIXI_INSTALL_DIR}"
      chmod +x "${PIXI_INSTALL_DIR}/pixi"

# ── macOS environments ────────────────────────────────────────────────────────
# image matches ^macos — no Docker container is used, runs on GHA macOS runners.
#
# macos-current: Bare macOS 26 — closest analog to a minimal Linux base image.
#   Homebrew is uninstalled via the official uninstaller, and user-level config
#   files are cleaned, so tests cannot rely on pre-installed state.
#   clean_path: strips the GHA runner's pre-installed PATH to the macOS system
#   baseline (/usr/bin:/bin:/usr/sbin:/sbin).
#   On native macOS, build.dockerfile is a shell bootstrap (see run.py), not Docker.
#
# macos-current+brew: macOS 26 with Homebrew available — matches the GHA runner's
#   default state. Homebrew lives outside the stripped PATH; scenarios and setup
#   scripts invoke `brew --prefix` and resolve the real binary
#   (arm64: /opt/homebrew, Intel: /usr/local).

macos-current:
  image: macos-26
  attributes: {os.id: macos, plat.pm: brew, plat.machine_release: [amd64, arm64]}
  clean_path: true
  build:
    dockerfile: |
      set -eux
      # Uninstall Homebrew to simulate a clean macOS environment.
      # install.sh bootstraps bash by compiling from source (Case 3 in
      # _ensure_bash4) — no pre-staged bash binary is needed here.
      _UNINSTALL_URL="https://raw.githubusercontent.com/Homebrew/install/HEAD/uninstall.sh"
      _UNINSTALL_TMP="$(mktemp /tmp/brew_uninstall.XXXXXX.sh)"
      curl -fsSL --retry 60 --retry-delay 5 --retry-connrefused "$_UNINSTALL_URL" -o "$_UNINSTALL_TMP"
      NONINTERACTIVE=1 bash "$_UNINSTALL_TMP" --force
      rm -f "$_UNINSTALL_TMP"
      # User configuration only (not ~/Library, Documents, .CFUserTextEncoding, …).
      rm -rf \
        "${HOME}/.config" \
        "${HOME}/.local" \
        "${HOME}/.cache" \
        "${HOME}/.docker" \
        "${HOME}/.ssh" \
        "${HOME}/.gnupg"
      rm -f \
        "${HOME}/.bash_profile" \
        "${HOME}/.bashrc" \
        "${HOME}/.bash_login" \
        "${HOME}/.profile" \
        "${HOME}/.zprofile" \
        "${HOME}/.zshrc" \
        "${HOME}/.zshenv" \
        "${HOME}/.zlogin" \
        "${HOME}/.shrc" \
        "${HOME}/.gitconfig" \
        "${HOME}/.gitignore_global" \
        "${HOME}/.gitattributes" \
        "${HOME}/.git-credentials" \
        "${HOME}/.netrc" \
        "${HOME}/.curlrc" \
        "${HOME}/.wgetrc" \
        "${HOME}/.wget-hsts" \
        "${HOME}/.inputrc" \
        "${HOME}/.vimrc" \
        "${HOME}/.npmrc" \
        "${HOME}/.yarnrc" \
        "${HOME}/.yarnrc.yml" \
        "${HOME}/.pypirc" \
        "${HOME}/.condarc"

macos-current+brew:
  image: macos-26
  attributes: {os.id: macos, plat.pm: brew, plat.machine_release: [amd64, arm64]}
  clean_path: true
  # Homebrew lives outside the stripped PATH; scenarios and setup scripts invoke
  # `brew --prefix` and must resolve the real binary (arm64: /opt/homebrew, Intel: /usr/local).
  path_prepend: /opt/homebrew/bin:/usr/local/bin
